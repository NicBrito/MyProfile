    {
        "extends": "@myprofile/typescript-config/tsconfig.json",
        "compilerOptions": {
        "outDir": "./dist", // Where the compiled JS files will be saved
        "rootDir": "./src", // Where our TS source code is located
        "noEmit": false // We need it to emit JS to run the server
        },
        "include": ["src"], // Which folders to compile
        "exclude": ["node_modules", "dist"]
    }